#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n,iterator;
    int arr[100];
    char string[100];
    printf("enter the input:");
    scanf("%d",&n);

    for(iterator=0;iterator<n;iterator++)
    {
        
        scanf("%d",&arr[i]);
        
    }

    getchar();
    
    scanf("%[^\n]%*c",string);

    printf("%d\n",n);
    for(iterator=0;iterator<n;iterator++)
    {
        printf("%d\n",arr[i]);
    }

    printf("%s\n",string);
    
    return 0;
}